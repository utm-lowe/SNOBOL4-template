-INCLUDE "time.sno"
	s = 0.25
	tries = 1 / s
	&code = 1
	t0 = tv_sec(gettimeofday())

top	output = lt(tries, 1) 'failed'			:s(end)
	tries = tries - 1
	sleep(s)					:f(top)
	t1 = tv_sec(gettimeofday())
	delta = t1 - t0
	eq(delta, 0)					:s(top)
	&code = 0
end

